<div class="row">
<%= form_for(membership_option, :html => {:class => "col-md-5"}) do |f| %>
  <% if membership_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(membership_option.errors.count, "error") %> prohibited this membership_option from being saved:</h2>

      <ul>
      <% membership_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :price %>
    <%= f.number_field :price, class:"form-control" %>
  </div>

  <div class="actions">
    <%= link_to 'Back', membership_options_path, class:"btn btn-default" %> <%= f.submit class:"btn btn-success" %>
  </div>
<% end %>
</div>
