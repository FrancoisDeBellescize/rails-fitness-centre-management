<%= form_for(@classe) do |f| %>
  <% if @classe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classe.errors.count, "error") %> prohibited this class from being saved:</h2>

      <ul>
      <% @classe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :day %>
    <%= select "classe", :day, ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"], class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :time %>
    <%= f.time_field :time, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :instructor_name %>
    <%= f.text_field :instructor_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :date_start %>
    <%= f.date_select :date_start, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class:"btn btn-default" %>
  </div>
<% end %>
